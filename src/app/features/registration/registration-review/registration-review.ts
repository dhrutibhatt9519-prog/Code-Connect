import { CurrencyPipe, DatePipe } from '@angular/common'; import { Component, input } from '@angular/core'; import { Opportunity, RegistrationPackage } from '../../../core/models/opportunity.model'; import { Participant } from '../../../core/models/registration.model';
@Component({selector:'app-registration-review',imports:[CurrencyPipe,DatePipe],templateUrl:'./registration-review.html',styleUrl:'./registration-review.scss'}) export class RegistrationReviewComponent{readonly opportunity=input.required<Opportunity>();readonly package=input.required<RegistrationPackage>();readonly quantity=input.required<number>();readonly participants=input.required<Participant[]>();}
